# Submission File

## What is included?
This application allows for the seeded users to create, edit, and share documents. The user can also upload different .txt and .md files (not .docx) for them to edit and share. The application allows for users to switch between the main user (Tanuja) and the reviwer user (Reviwer). The documents contents persist after reloading the page as well.